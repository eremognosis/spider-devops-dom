# cr45-reduced updated readme

> Note : This readme is written after containtrizing

## Tech Stack
- Vite Frontend (`/frontend`)
- Go backend (`/backend`)


## To run

```bash

docker compose up -d --build
```

It will run on `http://localhost` (reverse proxy)